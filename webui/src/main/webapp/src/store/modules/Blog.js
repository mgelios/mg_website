import blog from '../../api/blog';

const state = {
    categories : [],
    subcategories : []
};

const getters = {};

const actions = {};

const mutations = {};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
